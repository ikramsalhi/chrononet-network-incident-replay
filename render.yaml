services:
  - type: web
    name: chrononet-network-incident-replay
    runtime: python
    plan: free
    buildCommand: python -m compileall .
    startCommand: python app.py
    healthCheckPath: /api/health
    autoDeploy: true
